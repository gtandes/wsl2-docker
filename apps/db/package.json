{
  "name": "db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "docker run --rm --name hsh-db -p 5432:5432 -v ${PWD}/db-data:/var/lib/postgresql/data -e POSTGRES_PASSWORD=test123 -e POSTGRES_DB=ger -e POSTGRES_USER=ger postgres:14"
  }
}
